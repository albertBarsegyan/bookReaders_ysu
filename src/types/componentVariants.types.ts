export enum RegularModalVariants {
  ALERT = "alert",
  CONFIRM = "confirm",
  FORM = "form",
}

export enum RegularPopupVariants {
  SUCCESS = "success",
  DANGER = "danger",
  PRIMARY = "primary",
}
